# Release Runbook

## Goal

Define the repeatable path from "ready to ship" to "released with evidence."

## Minimum gates

- The relevant plan, review, QA, and score artifacts exist.
- Smoke coverage matches the primary user path.
- Rollback steps are written and plausible.
- Documentation matches behavior.

## Command slot

Replace the project-specific release command below after adoption:

`TODO(project): add the real release command sequence`
