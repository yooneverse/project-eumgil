<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!--
        제보 사진 첨부 시 갤러리 사진 picker 접근에 필요.
        Android 13+ (API 33): READ_MEDIA_IMAGES — 사진/비디오 분리된 미디어 권한.
        Android 12 이하 (API ≤ 32): READ_EXTERNAL_STORAGE — 통합 스토리지 권한 (maxSdkVersion 32로 제한).
        실제로는 PickMultipleVisualMedia (Photo Picker) 사용 시 권한 다이얼로그 없이도
        시스템이 사용자 선택을 위임하지만, 일부 디바이스/일부 URI 패턴에서는 권한이 요구되므로 선언.
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".app.BusanEumgilApp"
        android:allowAudioPlaybackCapture="true"
        android:allowBackup="true"
        android:icon="@drawable/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.BusanEumgil"
        android:usesCleartextTraffic="${USES_CLEARTEXT_TRAFFIC}">
        <activity
            android:name=".app.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.BusanEumgil.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="oauth"
                    android:scheme="kakao${KAKAO_NATIVE_APP_KEY}" />
            </intent-filter>
        </activity>
    </application>

</manifest>
