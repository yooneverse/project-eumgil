# 2026-05-12 제보 탭 실제 데이터 연동 계획

## 1. 문서 목적

- 제보 탭(REPORT-01~04)에서 실제 데이터 연동이 되지 않고 mock/shell/placeholder 값으로 채워져 있는 영역을 코드 기준으로 식별하고, Jira Story / Task 단위로 분기한다.
- 본 문서는 두 단계로 구성된다.
  - **§2~§4 코드 audit**: 현재 사실, 영향, 구현 방향을 file:line 기준으로 정리
  - **§5~§10 Jira 분기·실행 계획**: Story / Task 단위로 압축, 권장 처리 순서, 회귀 검증, Epic 연결, BE 협의 사항
- 코드 수정은 별도 Task / Branch에서 진행한다.
- 선행 컨텍스트: `FE/docs/qa/2026-05-08_제보_작성_QA_체크리스트.md`, `FE/docs/qa/2026-05-08_제보_완료_재진입_QA_체크리스트.md`는 ViewModel 로직 기준으로 "마감" 선언했으나, 실제 UI 입력 수단 대부분이 mock인 상태로 시연에서 동작하지 않음.

## 2. 범위

- 포함 범위
  - 제보 작성 4단계(REPORT-01 유형 → REPORT-02 위치 → REPORT-03 사진/설명 → REPORT-04 완료) UI 입력 수단 실연동
  - draft 충돌 시 사용자 확인 흐름
  - ViewModel이 emit하지만 Route에서 무시되는 UI 이벤트 연결
  - 서버 제출 페이로드 정합성(특히 `imageUrls`), 멱등성, 401 흐름
  - 마이페이지 제보 내역 카드 정보 보강
- 제외 범위
  - BE 신규 API 설계 (이미지 업로드, 멱등성 키 등은 §10에 협의 사항으로만 기록)
  - 마이페이지 제보 내역 화면 자체 재설계
  - 저시력 전용 화면 변형

## 3. 현재 FE 구현 사실 (코드 audit)

### 3.1 사진 첨부 — 완전 mock

- [현재 사실] `FE/app/src/main/java/com/ssafy/e102/eumgil/feature/report/ReportViewModel.kt:302-311`의 `addPhotoShell()`이 호출되며, 실제 picker가 아니라 가짜 URI `content://mock/report-photo-$nextIndex.jpg`와 고정 size `1_024_000`을 만들어 state에 주입한다.
- [현재 사실] `OpenPhotoPicker` 이벤트는 `FE/app/src/main/java/com/ssafy/e102/eumgil/feature/report/ReportContract.kt:253`에 선언만 있고, ViewModel에서 emit하는 곳이 없다.
- [현재 사실] `FE/app/src/main/java/com/ssafy/e102/eumgil/feature/report/ReportRoute.kt:78`에서 `OpenPhotoPicker -> Unit` 으로 무시된다.
- [현재 사실] `FE/app/src/main/java/com/ssafy/e102/eumgil/feature/report/ReportScreen.kt:951-956`의 `ReportPhotoThumb`은 썸네일 자리에 "사진" 텍스트만 표시한다. Coil 등 이미지 로더가 도입되지 않았다.
- [현재 사실] 카메라/갤러리 권한 manifest 항목과 `ActivityResultContracts` 연결이 없다.
- [현재 사실] `FE/app/src/main/java/com/ssafy/e102/eumgil/data/repository/ReportRepository.kt:242`에서 서버 제출 시 `imageUrls = emptyList()`를 항상 보낸다. 첨부된 사진 정보가 서버에 전혀 전달되지 않는다.
- [영향] 사용자가 사진을 첨부해도 가짜 URI만 쌓이고, 서버 등록 시 사진 0장으로 기록된다. 시연 시 "사진" 텍스트 박스만 노출되어 비정상적으로 보인다.

### 3.2 위치 / 지도 SDK — 하드코딩 좌표

- [현재 사실] `FE/app/src/main/java/com/ssafy/e102/eumgil/feature/report/ReportViewModel.kt:237-249`의 `setCurrentLocationShell()`은 "현재 위치로 설정" 버튼에 매핑되어 있으나, 항상 `lat=35.1796, lng=129.0756, address="부산광역시 부산진구 중앙대로 인근"` 을 반환한다.
- [현재 사실] `FE/app/src/main/java/com/ssafy/e102/eumgil/feature/report/ReportViewModel.kt:251-263`의 `setPickedLocationShell()`은 "지도에서 위치 선택" 버튼에 매핑되어 있으나, 항상 `lat=35.1578, lng=129.0592, address="부산광역시 부산진구 서면역 인근"`을 반환한다.
- [현재 사실] `FE/app/src/main/java/com/ssafy/e102/eumgil/feature/report/ReportScreen.kt:453-486`의 `ReportMapPlaceholder`는 회색 박스에 "지도 SDK 연결 전 placeholder입니다" 문구만 표시한다.
- [현재 사실] `FE/app/src/main/java/com/ssafy/e102/eumgil/core/location/CurrentLocationManager.kt`는 이미 존재하고 Map/Navigation 화면이 사용 중이나, ReportViewModel은 참조하지 않는다.
- [현재 사실] `FE/app/src/main/java/com/ssafy/e102/eumgil/feature/map/component/KakaoMapViewport.kt`는 Map 화면이 사용 중이지만 ReportScreen에 임베드되어 있지 않다.
- [현재 사실] `OpenLocationPicker`, `RequestLocationPermission` 이벤트는 `ReportContract.kt:249, 251`에 선언만 있고, emit·handle 모두 안 된다.
- [현재 사실] 좌표 → 주소 reverse geocoding이 없다. 사용자가 주소 텍스트를 직접 입력하지 않으면 영구히 좌표만 남는다.
- [영향] 어디서든 동일한 부산진구 좌표 2종만 사용되어, 실제 사용자 위치와 무관한 제보가 등록된다.

### 3.3 Draft + 새 제보 충돌 — confirm 다이얼로그 누락

- [현재 사실] `FE/app/src/main/java/com/ssafy/e102/eumgil/feature/report/ReportViewModel.kt:112-127`의 `loadLatestDraft()`는 앱 진입 시 `draftId`와 `hasExistingDraft=true`를 state에 채운다. 그러나 reportType/location/photo/description 값은 비어있다.
- [현재 사실] 사용자가 "불러오기"를 누르지 않고 곧바로 유형 카드를 클릭하면, `ReportViewModel.kt:210-227`의 `selectReportType()`은 reportType만 갱신하고 `draftId`/`hasExistingDraft`를 그대로 둔다.
- [현재 사실] 그 상태에서 사용자가 "임시저장"을 누르면 `toDraftData()`(`ReportViewModel.kt:592-612`)가 기존 draftId를 재사용하여 이전 draft를 덮어쓴다.
- [현재 사실] 또는 제출하면 `deleteDraftIfPresent()`(`ReportViewModel.kt:516-523`)가 기존 draft를 조용히 삭제한다.
- [현재 사실] `ShowDraftDiscardDialog` 이벤트는 `ReportContract.kt:247`에 선언만 있고, ViewModel·Route 모두에서 emit/handle하지 않는다.
- [영향] 사용자 입장에서 "새 제보를 작성한다"고 인식한 동작이 조용히 기존 draft를 손실시킨다.

### 3.4 ViewModel이 emit하지만 Route에서 무시되는 UI 이벤트

- [현재 사실] `FE/app/src/main/java/com/ssafy/e102/eumgil/feature/report/ReportRoute.kt:73-85`의 `handleReportUiEvent`는 아래 이벤트를 모두 `Unit`(무시)로 처리한다.

| 이벤트 | ViewModel emit 위치 | 무시되는 결과 |
| --- | --- | --- |
| `ShowSnackbar` | `ReportViewModel.kt` 9곳 (라인 161, 173, 195, 205, 435, 460, 491, 513) | 임시저장/제출/실패 메시지가 사용자에게 안 보임 |
| `AnnounceForAccessibility` | `ReportViewModel.kt` 3곳 (라인 162, 461, 492) | TalkBack 음성 안내 안 됨 |
| `NavigateToReportComplete` | `ReportViewModel.kt:462, 493` | 외부 reportId/outboxId handoff 끊김 (현재는 화면 내 step 전환만 동작) |
| `ScrollToFirstError` | `ReportViewModel.kt:531` | 폼이 긴 경우 첫 에러 위치로 스크롤 안 됨 |
| `ShowDraftDiscardDialog` | **emit하는 곳이 없음** | §3.3 문제의 근본 원인 |
| `OpenPhotoPicker` | emit하는 곳 없음 | §3.1 문제의 일부 |
| `OpenLocationPicker` | emit하는 곳 없음 | §3.2 문제의 일부 |
| `RequestLocationPermission` | emit하는 곳 없음 | §3.2 문제의 일부 |

- [현재 사실] `ReportScreen` Scaffold(`ReportScreen.kt:62-77`)에는 `snackbarHost` 파라미터가 지정되어 있지 않다.

### 3.5 NetworkMonitor — dead code

- [현재 사실] `FE/app/src/main/java/com/ssafy/e102/eumgil/feature/report/NetworkMonitor.kt:7-9`의 `DefaultNetworkMonitor.isOnline()`은 항상 `true`를 반환한다.
- [현재 사실] grep 결과 `NetworkMonitor` 인터페이스나 `DefaultNetworkMonitor` 객체를 참조하는 코드가 없다. 선언만 있는 dead code.
- [영향] 오프라인 상태에서 사용자에게 사전 안내가 없고, 제출 시도 후 네트워크 실패로만 분기된다.

### 3.6 Draft 사진 다장 영속화 한계

- [현재 사실] UI는 `ReportFormLimits.PHOTO_MAX_COUNT = 5`로 5장까지 허용한다(`ReportContract.kt:7`).
- [현재 사실] `FE/app/src/main/java/com/ssafy/e102/eumgil/data/local/entity/ReportDraftEntity.kt`의 draft 스키마는 `photoUri` 단일 필드만 가진다. `toDraftData()`(`ReportViewModel.kt:592-612`)는 `firstOrNull`만 저장한다.
- [현재 사실] `toUiState()`(`ReportViewModel.kt:648-657`)는 단일 `photoUri`를 1장 리스트로 복원한다.
- [영향] 사진 5장 첨부 후 임시저장하면 1장만 복원된다. (§3.1 사진 picker 미연동이 해결된 이후에야 실제 문제로 노출됨)

### 3.7 마이페이지 내역의 누락 필드

- [현재 사실] `FE/app/src/main/java/com/ssafy/e102/eumgil/data/repository/ReportRepository.kt:332-348`의 `HazardReportListItemDto.toHistoryData()`는 `description = null, address = null`로 고정한다. 서버 list API 응답에 해당 필드가 없기 때문이다.
- [현재 사실] 상세 진입(`fetchServerReportDetail`, 라인 324-330) 시에만 description이 채워진다. address는 상세에서도 null.
- [영향] 마이페이지 제보 내역 목록 카드에 설명/주소가 비어있고, 좌표만 표시된다.

## 4. 실제로 연동되어 동작하는 영역 (회귀 방지 참고)

mock이 아니라 실제 데이터로 정상 동작하는 영역. 작업 시 깨지지 않아야 할 대상.

- Room DB draft 저장/조회/삭제 — `ReportDraftDao`, `ReportRepository.saveDraft` (라인 180-197)
- Room DB outbox 저장 — `ReportOutboxDao`, `ReportRepository.saveOutbox` (라인 199-212)
- `POST /hazard-reports` 서버 제출 — `ReportRepository.submitOutboxToServer` (라인 214-265)
- 토큰 인증 — `accessTokenProvider` 주입 (라인 140)
- 멱등 재시도 — 이미 `Submitted`인 outbox는 새 호출 안 함 (라인 221-226)
- `GET /hazard-reports/me` 내역 조회 + 로컬 outbox merge — 라인 293-322
- `GET /hazard-reports/me/{id}` 상세 조회 — 라인 324-330
- draft → form 복원 step 자동 점프 — `toUiState()` 라인 659-664
- 4단계 step 전환 / 뒤로가기 / `TabReentered` 정책 — ViewModel
- 입력 validation (좌표 범위, 설명 300자, 사진 5장 cap) — `validateLocation`, `validateDescription`, `validatePhotos`
- 자동 테스트: `ReportViewModelTest` 34건, `MyPageReportHistoryViewModelTest` 5건

## 5. Jira 분기 — Story / Task 요약표

5주차 백로그 컨벤션을 따른다.

- `Task 1개 = 브랜치 1개 = MR 1개`
- 모든 Story Point는 `8 이하`, Task Point는 `1 / 3 / 5 / 8` 피보나치만 사용
- QA Task는 Story별로 분리하지 않고, 5개 Story 마감 후 단일 통합 QA Task로 처리 (§7 참고)
- 압축 기준: 동일 카테고리(UI 정합성 / 위치 입력 / 사진 첨부 / 저장 안정성 / BE 연동)별로 Story 1개씩 통합. Story Point 8 cap을 깨지 않는 한도에서 최소 단위로 압축

| # | Story | Task 수 | Story SP | 우선순위 | BE 의존 | 비고 |
| ---: | --- | ---: | ---: | --- | --- | --- |
| 1 | 제보 UI 정합성 마감 (피드백 채널 + Draft 충돌 + 단계별 컨텍스트) | 3 | 6 | Highest | 없음 | 후속 Story 검증 채널 확보 + 사용자 컨텍스트 인지 가능성 |
| 2 | 제보 위치 입력 실연동 (GPS + 지도 + RGC) | 3 | 8 | Highest | 없음 | SP cap 정확히 맞음 |
| 3 | 제보 사진 첨부 실연동 (Coil + Picker + 5장 영속화) | 2 | 7 | High | 없음 | Picker + Draft 다장 Room migration 동반. 3.1과 3.2는 인접 PR로 |
| 4 | 제보 저장 안정성 마감 (Outbox + 잔여 cleanup) | 4 | 5 | High | 없음 | dead code/테스트 정리 (Draft 영속화는 Story 3로 이관) |
| 5 | 제보 BE 연동 (협의 + 후속 5종) | 9 | 보류 | High | **있음** | 협의 Task 4건 분리 |

- 총 Story 수: **5**
- 총 Task 수: **21** (Task 4.3을 Task 3.2에 흡수)
- FE 단독 Story 1~4: Task 12건, 총 SP 26
- BE 연동 Story 5: Task 9건 (협의 4 + FE 후속 5), SP는 협의 결과 후 산정

## 6. Story별 상세 Task

### Story 1. 제보 UI 정합성 마감 (피드백 채널 + Draft 충돌 + 단계별 컨텍스트)

- Story 설명: ViewModel이 emit하지만 `ReportRoute.handleReportUiEvent`에서 `Unit`으로 무시되는 UI 이벤트(Snackbar 9곳, 접근성 안내 3곳, ScrollToFirstError 1곳) 채널과, draft 존재 시 새 유형 클릭 시 confirm 다이얼로그, 그리고 작성 도중 사용자가 "지금 어느 유형의 제보를 작성 중인지" 인지할 수 있도록 단계별 TopBar에 type 라벨을 병기하는 작업을 한 Story로 묶는다. 후속 Story들의 동작 검증과 사용자 피드백의 전제 조건이 된다.
- 완료 기준
  - 임시저장 / 제출 / 실패 / 폐기 시 Snackbar가 사용자에게 실제 노출됨
  - TalkBack 활성 시 음성 안내 전달됨
  - 폼 검증 실패 시 첫 에러 필드로 자동 스크롤됨
  - draft 존재 상태에서 새 유형 카드 클릭 시 confirm 다이얼로그 노출되고, 두 선택지(삭제 후 새로 작성 / 이어서 작성) 모두 정합성 통과
  - LocationConfirm / DetailInput 단계의 TopBar에 현재 선택된 type 라벨이 함께 노출됨 ("위치 확인 · 계단·단차 있음" 형태)

| Task | Task Point | 우선순위 | Branch 이름 제안 | 완료 기준 |
| --- | ---: | --- | --- | --- |
| Task 1.1 — ReportRoute UI 이벤트 채널 일괄 연결 | 3 | Highest | `fe/feat/report-ui-event-channel-{TaskNo}` | `ReportScreen` Scaffold에 `snackbarHost` 슬롯 추가, `SnackbarHostState`·scroll state hoist, `handleReportUiEvent`의 `ShowSnackbar` / `AnnounceForAccessibility` / `ScrollToFirstError` 분기 실제 처리. `NavigateToReportComplete`는 현재 step 전환과 중복이며 테스트 2건이 emit을 검증 중이라 본 Task에서는 유지 (후속 정리 대상). |
| Task 1.2 — Draft 충돌 confirm 다이얼로그 도입 | 2 | Highest | `fe/feat/report-draft-conflict-dialog-{TaskNo}` | `ReportUiAction`에 `DiscardDraftAndStartNew(type)`, `ResumeDraftFromDialog` 추가. `selectReportType()`에 충돌 감지 후 `ShowDraftDiscardDialog` emit. Route에서 AlertDialog 노출. 문구: "기존에 작성하던 내용이 있습니다. 삭제하고 새로 작성하시겠습니까?". 두 액션 처리 로직(`deleteDraft` → `resetForm` → `selectReportType` / `resumeDraft`) 추가. 충돌 감지 조건은 `hasExistingDraft && draftId != null` (in-memory state 무관), 같은 type 재클릭은 idempotent 처리. |
| Task 1.3 — 단계별 TopBar에 선택 type 라벨 병기 | 1 | High | `fe/feat/report-step-type-context-{TaskNo}` | [ReportScreen.kt:1195-1201](FE/app/src/main/java/com/ssafy/e102/eumgil/feature/report/ReportScreen.kt) `reportStepTitle()` 함수가 `step: ReportStep` 외에 `selectedType: ReportType?`도 받도록 시그니처 확장. LocationConfirm / DetailInput 단계에서 type이 있으면 "위치 확인 · 계단·단차 있음" 형태로 병기, 없으면 기존 라벨 유지. TypeSelection 단계는 type 선택 전이므로 그대로 "제보". Complete 단계는 본문 요약 카드와 중복이라 그대로 "제보 완료" 유지. `EumCenteredTopBar` 자체는 수정하지 않음 (단일 `title` 파라미터 그대로 사용). 회귀 가드 테스트: `reportStepTitle(LocationConfirm, RAMP)` 같은 조합 → 기대 문자열. |

### Story 2. 제보 위치 입력 실연동 (GPS + 지도 + Reverse Geocoding)

- Story 설명: REPORT-02 위치 단계의 "현재 위치로 설정" / "지도에서 위치 선택" 두 버튼이 모두 하드코딩 좌표를 반환하는 상태를 해소한다. 같은 화면, 같은 `selectLocation()` 함수를 공유하므로 단일 Story로 묶고, 작업 무게에 따라 3개 Task로 분리한다.
- 완료 기준
  - "현재 위치로 설정" 버튼이 실제 디바이스 GPS 좌표 반환
  - "지도에서 위치 선택" 버튼이 카카오맵을 띄우고, 핀 이동으로 임의 좌표 선택 가능
  - 선택된 좌표가 주소로 자동 변환되어 입력란에 반영 (실패 시 좌표만 유지, 사용자 직접 입력 가능)
  - 위치 권한 거부 / fetch 실패 / RGC 실패 모두 명확한 에러 처리

| Task | Task Point | 우선순위 | Branch 이름 제안 | 완료 기준 |
| --- | ---: | --- | --- | --- |
| Task 2.1 — 현재 위치 GPS 연동 (`CurrentLocationManager` + 권한 흐름) | 3 | Highest | `fe/feat/report-current-location-{TaskNo}` | `CurrentLocationManager`를 `ReportViewModel.provideFactory`에 주입, `AppContainer` wiring. `setCurrentLocationShell()` 제거 → `requestCurrentLocation()` 신규. 권한 체크 → `RequestLocationPermission` emit → `ActivityResultContracts.RequestPermission` → 권한 ok 시 `refreshLatestLocation()` + collect → `selectLocation(source = CurrentLocation)`. `isResolvingCurrentLocation` 로딩 UI 표시. 권한 거부 → `PermissionDenied`, fetch 실패 → `CurrentLocationUnavailable`. manifest의 `ACCESS_FINE_LOCATION`·`ACCESS_COARSE_LOCATION`은 이미 선언되어 있어 추가 없음. |
| Task 2.2 — KakaoMap 임베드 + 핀 위치 선택 | 5 | Highest | `fe/feat/report-map-embed-{TaskNo}` | `ReportScreen.kt:453-486`의 `ReportMapPlaceholder` 제거. `KakaoMapViewport` inline 임베드 (Map 화면이 사용 중인 컴포넌트 재사용). 초기 카메라는 Task 2.1의 현재 위치(있으면) 또는 기본값. 핀 이동 시 `LocationSelected(source = MapPin)` emit. `setPickedLocationShell()` 제거. |
| Task 2.3 — 좌표 → 주소 reverse geocoding | 2 | High | `fe/feat/report-reverse-geocoding-{TaskNo}` | 카카오 로컬 API `coord2Address` 호출 wrapper 신규 (코드베이스에 기존 wrapper 없음 확인). 위치 선택 시 자동 호출하여 주소 입력란 자동 채움. API 키 관리 정책 팀 합의 후 적용. 실패 시 좌표만 유지, 사용자 직접 입력 fallback. |

- Task 의존성: 2.2는 2.1과 독립 머지 가능. 2.3은 2.1 또는 2.2 머지 후 진행 권장 (호출처가 필요).

### Story 3. 제보 사진 첨부 실연동 (Coil + Photo Picker + Draft 다장 영속화)

- Story 설명: REPORT-03 사진 첨부가 가짜 URI를 생성하는 상태를 해소한다. Coil 도입과 picker 연동은 강결합(Coil 없으면 썸네일 깨짐, picker 없으면 Coil 호출 대상 없음)이므로 같은 PR 흐름으로 진행한다. Picker가 동작하는 순간 사용자는 즉시 5장 첨부 → 임시저장 → 재진입 흐름을 만들 수 있고 그 시점에 Draft 단일 photoUri 스키마의 영속화 누락이 노출되므로, **Room migration도 같은 Story 안 Task 3.2에 묶어 처리**한다 (별도 분리 시 시연 직전 데이터 손실 사고 위험).
- 완료 기준
  - "+" 버튼이 실제 디바이스 갤러리/카메라를 띄움
  - 선택된 사진의 실제 썸네일이 표시됨
  - 앱 재시작 후 draft 복원 시 **첨부한 5장 사진 모두 복원됨**
  - 기존 draft가 Room migration 시 손상되지 않음
  - 마이페이지 제보 내역 카드의 썸네일도 실제 이미지로 표시됨

| Task | Task Point | 우선순위 | Branch 이름 제안 | 완료 기준 |
| --- | ---: | --- | --- | --- |
| Task 3.1 — Coil 의존성 도입 + 썸네일 컴포넌트 교체 | 2 | High | `fe/feat/report-coil-thumb-{TaskNo}` | `FE/app/build.gradle.kts`에 Coil 의존성 추가 (현재 Coil/Glide/Picasso 없음 확인). `ReportScreen.kt:951-956`의 `ReportPhotoThumb`을 `AsyncImage`로 교체. 로딩 placeholder, 에러 fallback. 마이페이지 카드 컴포넌트도 동일 교체. ⚠ 단독 머지 시 mock URI는 깨지므로 Task 3.2와 같은 sprint에서 처리. |
| Task 3.2 — Photo Picker 연동 + manifest 권한 + Draft 5장 영속화 Room migration | 5 | High | `fe/feat/report-photo-picker-{TaskNo}` | **(Picker 부분)** `AndroidManifest.xml`에 `READ_MEDIA_IMAGES`(API 33+) / `READ_EXTERNAL_STORAGE`(API 32 이하) 권한 추가. `addPhotoShell()` 제거 → `OpenPhotoPicker` emit. Route에서 `ActivityResultContracts.PickMultipleVisualMedia` 호출. 선택된 Uri에 `takePersistableUriPermission` 적용. MIME / size 검증 후 `PhotoSelected` dispatch. 5장 cap 초과 시 Snackbar 안내 (Story 1의 채널 사용). **(Draft 영속화 부분)** 스키마 옵션 B(단일 컬럼 JSON 배열 + TypeConverter) 적용. `ReportDraftEntity` 스키마 확장, Room DB 버전 증가, migration 작성. migration 테스트 추가(기존 draft 보존 검증). `toDraftData()` / `toUiState()` / `toEntity()` 사진 리스트 처리로 변경. **PR 안에서 commit 단위 분리 권장**: `feat: Photo Picker 연동` + `feat: Draft 사진 다장 영속화 Room migration`. |

- Task 의존성: 3.1 → 3.2 순서. 같은 sprint에 묶어 동시 머지 권장.
- 합리화 사유: Task 3.2에 Draft 영속화를 흡수하는 이유 — Picker가 사용자에게 풀리는 순간 5장 첨부가 즉시 가능해지므로 영속화 누락은 별도 Task로 분리 시 시연 직전에 노출될 수 있는 데이터 손실 사고. 같은 PR에서 시연 안전 영역까지 끌고 가는 게 운영상 안전.

### Story 4. 제보 저장 안정성 마감 (Outbox + 잔여 cleanup)

- Story 설명: 저장 인프라(Room DB) 관련 잔여 정합성과 함께, Task 1.1·기존 lowvision 작업에서 미처리로 남은 dead code / 깨진 테스트를 한 번에 정리한다. NetworkMonitor dead code 활성화, Submitting outbox cleanup, `NavigateToReportComplete` dead emit 제거, LowVisionBottomNavTest 컴파일 에러 해소 모두 작은 변경이라 묶어서 처리한다. (Draft 사진 5장 영속화는 Picker가 풀리는 순간 즉시 데이터 손실 위험이 노출되므로 Story 3 Task 3.2로 이관됨.)
- 완료 기준
  - 오프라인 상태에서 제출 시도 시 사전 안내 표시, outbox는 정상 저장되어 네트워크 복구 후 재시도 가능
  - 앱 시작 시 `status=Submitting`인 outbox가 `Pending`으로 자동 복구
  - dead emit 및 깨진 테스트 제거되어 `gradlew :app:testDebugUnitTest` 전체 실행 가능

| Task | Task Point | 우선순위 | Branch 이름 제안 | 완료 기준 |
| --- | ---: | --- | --- | --- |
| Task 4.1 — `AndroidNetworkMonitor` 실구현 + submitReport 오프라인 분기 | 2 | High | `fe/feat/report-network-monitor-{TaskNo}` | `ConnectivityManager` 기반 `AndroidNetworkMonitor` 작성, `AppContainer` wiring (현재 `DefaultNetworkMonitor`는 dead code이므로 사용처 신규). `ReportViewModel.submitReport()` 진입 시 `isOnline()` 체크. 오프라인이면 outbox 저장 후 서버 호출 skip, Snackbar 안내 "오프라인 상태입니다. 네트워크 복구 후 다시 시도해 주세요". |
| Task 4.2 — 앱 시작 시 Submitting outbox cleanup | 1 | High | `fe/feat/report-outbox-cleanup-{TaskNo}` | `AppContainer` 초기화 또는 별도 startup job에서 `status=Submitting`인 outbox를 `Pending`으로 업데이트. 로그 기록. |
| Task 4.4 — `NavigateToReportComplete` dead emit 제거 | 1 | Low | `fe/feat/report-complete-event-cleanup-{TaskNo}` | Task 1.1에서 보류한 cleanup. `ReportViewModel.kt:462, 493`의 emit 2건 제거, `ReportContract.kt`의 `NavigateToReportComplete` 데이터 클래스 제거, `ReportRoute.kt`의 처리 분기 제거, `ReportViewModelTest.kt:218-219, 241`의 검증 2건도 함께 정리(state 기반 검증으로 교체). 현재 화면 내 step 전환은 `state.currentStep`으로 이미 처리되므로 사용자 동작 영향 없음. |
| Task 4.5 — LowVisionBottomNavTest 컴파일 에러 해소 | 1 | High | `fe/feat/lowvision-bottom-nav-test-fix-{TaskNo}` | `LowVisionBottomNavTest.kt:25`의 `lowVisionVoiceInputBottomNavBackgroundColor()` 미해결 참조 해소. git blame으로 production 함수 제거 시점 확인 후 (a) 함수 복구 또는 (b) 테스트 라인 제거 결정. develop 기준에서도 이미 깨져 있는 사전 존재 이슈로 `gradlew :app:testDebugUnitTest` 전체 실행을 차단 중이라, **가능하면 본 Story 내에서 일찍 처리**하여 CI/회귀 검증 채널을 복구한다. |

- Task 의존성: 4.1, 4.2, 4.4, 4.5 모두 독립.
- Task 4.4와 4.5는 본 Story에 카테고리상 합류된 cleanup 작업으로, Story 1·2·3의 시연 마감선과는 무관하다.

### Story 5. 제보 BE 연동 (협의 + 후속 5종)

- Story 설명: BE 명세 변경 또는 신규 API가 선행되어야 FE 작업이 가능한 항목 5종(이미지 업로드, 멱등성, 401, 마이페이지 list 응답)을 한 Story로 묶는다. 협의 Task 4건은 BE 협의 진행률 가시화 목적으로 같은 Story 내 분리 Task로 유지한다.
- 완료 기준
  - BE와 4종 명세(이미지 업로드 / list 응답 확장 / 멱등성 / 401 refresh) 모두 합의 문서화
  - 첨부 사진이 서버 detail 응답의 `imageUrls`로 노출됨
  - 마이페이지 카드에 주소·설명 미리보기 노출
  - 재시도 시 멱등성 보장
  - 토큰 만료 시 자동 갱신 또는 명확한 로그인 화면 navigate
- 진행 정책: 협의 Task(5.1~5.4)는 FE 단독 Story 1~4와 병렬 착수. 협의 완료 순서대로 후속 FE Task(5.5~5.9) 진입.

| Task | Task Point | 우선순위 | Branch 이름 제안 | 완료 기준 |
| --- | ---: | --- | --- | --- |
| Task 5.1 — [BE 협의] 이미지 업로드 엔드포인트 명세 합의 | 1 | Highest | (협의 Task, 브랜치 없음) | 업로드 방식(S3 presigned vs multipart), 업로드 시점, 실패 정책, `imageUrls` cap, 고아 사진 청소 정책 결정. 문서화. |
| Task 5.2 — [BE 협의] `GET /hazard-reports/me` 응답에 address / description preview 포함 요청 | 1 | High | (협의 Task) | list 응답에 `address` 필드 추가, `description`은 preview 형태(50~80자 + "...") 포함. 전체 description은 detail API에서 유지. 명세 확정. |
| Task 5.3 — [BE 협의] 멱등성 정책 합의 | 1 | High | (협의 Task) | `Idempotency-Key` 헤더 도입 vs `outboxId` 기반 중복 차단 중 결정. 키 만료 정책 합의. 문서화. |
| Task 5.4 — [BE + 인증 도메인 협의] refresh token 흐름 설계 | 1 | High | (협의 Task) | BE refresh token API 명세, FE OkHttp Interceptor 위치, 토큰 갱신 트리거, 로그인 navigate 방식 결정. 인증 도메인 담당자 cross-cutting 합의. |
| Task 5.5 — 이미지 업로드 RemoteDataSource + outbox URL 보존 | 3 | High | `fe/feat/report-image-upload-{TaskNo}` | 5.1 명세에 따라 업로드 호출 구현. `ReportOutboxEntity`에 `uploadedImageUrls` 필드 추가, Room migration 동반(Task 4.3과 같은 sprint에 묶으면 migration 한 번에 정리). 업로드 성공 시 URL 저장, 실패 시 재시도 가능 상태 보존. |
| Task 5.6 — `submitOutboxToServer`에서 `imageUrls` 실제 전송 | 1 | High | `fe/feat/report-image-urls-submit-{TaskNo}` | `ReportRepository.kt:242`의 `emptyList()`를 outbox의 `uploadedImageUrls`로 교체. 업로드 미완료 시 제출 차단 또는 사용자 안내. |
| Task 5.7 — 마이페이지 내역 카드 UI 업데이트 | 1 | Medium | `fe/feat/report-history-card-info-{TaskNo}` | `HazardReportListItemDto.toHistoryData()` (라인 332-348)에서 `address`, `description` preview 매핑. 카드 컴포넌트에 주소/설명 미리보기 표시. preview 길이 초과 시 말줄임표 처리. |
| Task 5.8 — 제출 호출에 멱등성 키 추가 | 1 | Medium | `fe/feat/report-idempotency-key-{TaskNo}` | `HazardReportsRemoteDataSource` 제출 호출에 헤더 또는 body 필드로 멱등성 키 추가. `outboxId` 또는 별도 UUID 사용(5.3 합의에 따름). |
| Task 5.9 — 401 처리 흐름 구현 | 2 | Medium | `fe/feat/report-401-refresh-{TaskNo}` | 5.4 합의에 따라 OkHttp Interceptor 또는 별도 핸들러 구현. 모든 도메인 일관 적용(제보뿐 아니라 마이페이지·지도 등). |

- Task 의존성: 5.5 ← 5.1, 5.7 ← 5.2, 5.8 ← 5.3, 5.9 ← 5.4. 5.6은 5.5 머지 후.

## 7. 권장 처리 순서

| 순서 | Task | 사유 |
| ---: | --- | --- |
| 1 | Task 1.1 (UI 이벤트 채널) | Story 2~4 동작 검증의 피드백 채널 확보 |
| 2 | Task 1.2 (Draft 충돌 다이얼로그) | 데이터 손실 방지가 가장 시급 |
| 3 | **Task 1.3 (단계별 TopBar type 라벨 병기)** | 작성 도중 사용자가 어느 유형으로 작성 중인지 인지 불가 → 시연 가독성 차원에서 시연 마감선 안에서 처리 |
| 4 | **Task 4.5 (LowVisionBottomNavTest 컴파일 에러 해소)** | `gradlew :app:testDebugUnitTest` 차단 해소 → 후속 Task의 CI 회귀 가드 복구. 가장 빨리 처리할수록 이득 큼 |
| 5 | Task 5.1~5.4 (BE 협의 4건 동시 착수) | 협의는 미팅 일정에 의존하므로 FE 단독 작업과 병렬 진행 |
| 6 | Task 2.1 (현재 위치 GPS) | 인프라 이미 존재, 권한 흐름 안정화 |
| 7 | Task 2.2 (KakaoMap 임베드) | 2.1 위에 쌓음, 위치 입력 핵심 |
| 8 | Task 2.3 (Reverse geocoding) | 좌표만 표시되는 문제 해소 |
| 9 | Task 3.1 + 3.2 (Coil + Picker + Draft 5장 영속화, 같은 sprint) | 사진 입력 수단의 핵심, 강결합이라 같이 처리. Picker 머지 시점에 5장 영속화도 함께 보장 |
| 10 | Task 4.1 (NetworkMonitor) | UX 보강 |
| 11 | Task 4.2 (Outbox cleanup) | 좀비 outbox 제거 |
| 12 | **Task 4.4 (`NavigateToReportComplete` dead emit 제거)** | Task 1.1 보류분 정리. 사용자 동작 영향 없으므로 후순위 |
| 13 | Task 5.5~5.9 (BE 협의 완료된 순서대로) | 명세 확정 순서대로 FE 후속 진입 |

- **시연 가능 마감선:** Task 1.1 ~ Task 3.2까지 완료 시점 (Story 1 / 2 / 3 마감).
- Task 4.x, 5.x는 시연 후 후속 정리 가능. 단 **Task 4.5만은 가능한 한 일찍** 처리하여 unit test 자동 실행 채널을 복구한다.

## 8. Story 별 회귀 검증 항목 (마지막 통합 QA Task에 묶음)

QA Task는 본 분기계획에서 Story별로 분리하지 않고, 5개 Story가 모두 마감된 후 단일 통합 QA Task로 처리한다. 회귀 검증 시 확인해야 할 핵심 항목은 다음과 같다.

| Story | 회귀 검증 항목 |
| --- | --- |
| Story 1 | 모든 Snackbar 메시지 실제 노출 / TalkBack 음성 안내 / 첫 에러 스크롤 / draft confirm 다이얼로그 두 선택지 모두 정합성 / LocationConfirm·DetailInput TopBar에 type 라벨 병기 노출 (TypeSelection·Complete 단계는 기존 라벨 유지) |
| Story 2 | 권한 거부·허용·미응답 케이스 / 위치 fetch 실패 / 지도 핀 이동 / RGC 성공·실패·timeout / 부산 외 지역 좌표 |
| Story 3 | 갤러리·카메라 picker / 5장 cap / persistable URI 권한 / 마이페이지 카드 썸네일 / **5장 첨부 후 임시저장·앱 재시작·이어하기 시 5장 모두 복원 (Task 3.2 흡수분)** / **기존 draft가 Room migration 시 손상 없음 (Task 3.2 흡수분)** |
| Story 4 | 비행기 모드 제출 / 앱 강제 종료 후 재시작 outbox 상태 / 제출 성공 후 완료 화면 진입 정상(Task 4.4 회귀 확인) / `gradlew :app:testDebugUnitTest` 전체 PASS(Task 4.5 회귀 확인) |
| Story 5 | 사진 업로드 성공·실패·부분 성공 / 마이페이지 카드 description preview / 멱등성(같은 요청 2회) / 토큰 만료 후 자동 갱신 |

기존 자동 테스트(`ReportViewModelTest` 34건, `MyPageReportHistoryViewModelTest` 5건)는 본 작업으로 깨지지 않아야 하며, Task별로 신규 케이스가 필요하다.

## 9. Epic 연결

- Story 1 / 2 / 3 / 4 / 5 모두 `S14P31E102-194` 제보 및 로컬 저장 기반 구축 Epic 하위에 연결한다.
- BE API 명세는 `S14P31E102-251` (제보 API)에 `연결 이슈`로 추가한다.
- 인증 cross-cutting 사항(Task 5.4 / 5.9)은 `S14P31E102-303` (로그인 및 온보딩)에 `연결 이슈`로 추가한다.

## 10. 백엔드 개발자에게 전달할 작업사항

§6 Story 5의 협의 Task 4건과 매핑된다.

| 항목 | 요청 내용 | 사유 / 매핑 |
| --- | --- | --- |
| 이미지 업로드 엔드포인트 | S3 presigned URL 발급 API 또는 multipart 업로드 API 명세 | Task 5.1 차단. FE는 URL 기반 전송 가능 상태 |
| `imageUrls` cap 확인 | 1개 / 5개 / 무제한 정책 확정 | UI는 5장 cap이지만 서버 정책 미확정 (Task 5.1 부속) |
| 멱등성 키 | `Idempotency-Key` 헤더 도입 또는 outboxId 기반 중복 차단 | Task 5.3. 네트워크 race로 중복 등록 가능 |
| list 응답 확장 | `GET /hazard-reports/me` 응답에 description, address 포함 | Task 5.2. 마이페이지 내역 카드에 정보 부족 |
| 401 후속 정책 | 토큰 만료 시 refresh 흐름과 FE 동작 가이드 | Task 5.4. 자동 재로그인 흐름이 인증 도메인과 cross-cutting |

## 11. 관련 문서

- 선행 spec: `FE/docs/sprint_backlog/archive/2026-04-20_week3/2026-04-21_S14P31E102-204_제보_작성_폼_state_spec.md`
- Story 2 QA (기존 마감 기준): `FE/docs/qa/2026-05-08_제보_작성_QA_체크리스트.md`
- Story 3 QA (기존 마감 기준): `FE/docs/qa/2026-05-08_제보_완료_재진입_QA_체크리스트.md`
- 5주차 백로그: `FE/docs/sprint_backlog/2026-05-04_FE_박세홍_업무_정리.md`
- API 명세: `Docs/API/사용자_도메인/2026-04-12_제보_API_명세.md`
